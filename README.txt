# Comtext

Community SMS list management

## Installation

1. Install node
2. Install npm
3. Run `npm install`
4. Update settings `cp lib/settings/settings.example.js lib/settings/settings.js` 
4. Run: `node lib/server.js`